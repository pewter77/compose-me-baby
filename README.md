# Composing

Docker-Compose Files for my Self-hosting needs

```
Docker Format:
    service-name:
        image:
        container_name:
        depends_on:
        hostname:
        user:
        privileged:
        expose:
        ports:
        network_mode:
        networks:
        environment:
        env_file:
        volumes:
        logging:
        devices:
        restart:
        command:
        labels:
        healthcheck:
        cpus:
        mem_limit:
        {others go here}
```
